slack.team=
slack.token=
slack.url=https://${slack.team}.slack.com/services/hooks/incoming-webhook?token=${slack.token}
slack.channel=
slack.payload={"text": "${slack.text}", "channel": "${slack.channel}"}
