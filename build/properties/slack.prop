; Url comes from Slack Incoming WebHooks configuration.
slack.enabled=false
slack.url=https://hooks.slack.com/services/
slack.channel=
slack.payload={"text": "${slack.text}", "channel": "${slack.channel}"}
